#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

sudo install -d -m 0755 /etc/nix
sudo install -m 0644 "$repo_root/system/etc/nix/nix.conf" /etc/nix/nix.conf
